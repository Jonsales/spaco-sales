## 1. Map styling

- [x] 1.1 Remove the `filter` property from `.map-embed` in `map.css`; verify no `filter` declaration remains anywhere in `map.css`
- [x] 1.2 Open the page and confirm the map renders in Google's native colors (no gray/sepia tint); verify visually
