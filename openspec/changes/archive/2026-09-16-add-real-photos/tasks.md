## 1. CSS: prep framing classes for real images

- [x] 1.1 Add one rule to `assets/css/styles.css` applying `width:100%;height:100%;object-fit:cover;display:block` to every framing class (`.image-hero`, `.image-tall`, `.image-wide`, `.image-space-1..7`, `.image-birthday`, `.image-detail`, `.quote-image`) plus a `.gallery-item img` rule (since gallery sizing lives on the `g1..g6` button, not the inner image); verify the rule doesn't affect the still-placeholder blocks (experience/final/instagram) since those keep their `<div>` wrapper. **Correction during implementation:** `.quote-image` was missing from the original task list even though task 2.7 puts it directly on an `<img>` — added it here.

## 2. Replace placeholders with real photos

- [x] 2.1 Hero (`#top` hero section): replace `<div class="hero-media image-placeholder image-hero">...</div>` with `<img class="hero-media image-hero" src="assets/img/hero.JPG" alt="...">`; verify the hero photo fills the hero banner
- [x] 2.2 Editorial photo (`#espaco`): replace with `<img class="image-tall" src="assets/img/foto-espaco-01.jpeg" alt="...">`, keeping the `photo-note` caption below it; verify layout unchanged
- [x] 2.3 Structure cards 01–07 (`#estrutura`): replace each `image-space-N` placeholder with its matching photo (`salao_principal.jpg`, `area_externa.JPG`, `pergolado.JPG`, `piscina.JPG`, `churrasqueira.jpeg`, `cozinha.jpeg`, `estacionamento.jpeg`); verify all 7 cards show distinct photos
- [x] 2.4 Birthday photo (`#eventos`): replace with `<img class="birthday-photo image-birthday" src="assets/img/aniversario.jpeg" alt="...">`; verify it fills the birthday section's photo column
- [x] 2.5 Gallery g1–g6 (`#galeria`): replace each `gallery-item`'s inner placeholder with `<img src="assets/img/foto-0N.<ext>">` (exact case per file), keeping the button/lightbox wiring (`data-title`) intact; verify the lightbox still opens each photo correctly (`script.js` reads the clicked item, not the image itself, so no JS change expected)
- [x] 2.6 Details photo: replace with `<img class="details-photo image-detail" src="assets/img/foto-madeira.JPG" alt="...">`; verify layout unchanged
- [x] 2.7 Quote section photo: replace with `<img class="quote-image" src="assets/img/foto-cinematografica.jpeg" alt="...">`; verify the dark overlay/text still read clearly on top of it

## 3. Verification

- [x] 3.1 Confirm the 3 explicitly-deferred blocks (experience "panorâmica", final-CTA, and the 4 Instagram tiles) are untouched and still show as placeholders
- [x] 3.2 Open the page and check all 18 replaced photos render, are cropped (not stretched), and no broken-image icons appear (wrong filename case, wrong path). **Correction after visual review:** the structure cards (`#estrutura`) rendered at full native size, breaking the layout — `.space-card .image-placeholder` / `.space-card-large .image-placeholder` in `assets/css/styles.css` scoped `aspect-ratio` to the `.image-placeholder` class specifically, which the new `<img class="image-space-N">` tags no longer carry. Fixed by adding `.space-card img` / `.space-card-large img` (desktop and the 900px breakpoint) alongside the existing `.image-placeholder` selectors, so both the old placeholder pattern and the new real `<img>` get the same `aspect-ratio`/hover-transform. **Second correction:** that first fix alone wasn't enough — the generic framing-class rule from task 1.1 also set `height:100%` on these same images, which fights with `aspect-ratio` when the parent has no definite height (the structure cards' grid row is auto-sized, not stretched). Split that rule in two: `.image-tall`, `.image-space-1..7`, `.image-detail` (aspect-ratio-driven, static flow) now get only `width:100%;object-fit:cover;display:block` — no `height:100%` — while `.image-hero`, `.image-wide`, `.image-birthday`, `.quote-image` (which fill an absolutely-positioned or explicitly-sized container) keep `width:100%;height:100%`.
